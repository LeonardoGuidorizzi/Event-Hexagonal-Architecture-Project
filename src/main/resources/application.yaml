spring:
  application:
    name: Event

  datasource:
    url: jdbc:postgresql://localhost:5431/EventDB
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    location: classpath:db/migration